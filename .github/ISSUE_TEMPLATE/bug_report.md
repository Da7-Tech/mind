---
name: Bug report
about: Something behaved differently from what the docs promise
labels: bug
---

**What happened**

**What you expected** (quote the README/SKILL line if possible)

**Repro** (commands against a fresh `mind.py init` in a temp dir — the
whole tool is one file, so a repro is usually 3-5 lines)

```bash

```

**Environment**: OS / Python version / mind version (`python3 mind.py --version`)

**`.mind/` state if relevant** (graph.json is JSON; journal.jsonl explains
every mutation — paste the relevant lines, redact anything private)
